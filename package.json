{
  "name": "velo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VELO — zero-knowledge forensic attestation on Midnight",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p .",
    "test": "tsc -p . && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
